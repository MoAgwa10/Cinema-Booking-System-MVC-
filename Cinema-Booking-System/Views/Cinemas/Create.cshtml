@model Cinema_Booking_System.Models.ViewModels.CinemaCreateVM

@{
    ViewData["Title"] = "Create Cinema";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h4>Add a New Cinema</h4>
        </p>

        <div class="row">
            <div class="col-md-8 offset-2">
                
                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="bi bi-exclamation-triangle me-2"></i>@TempData["Error"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }
                
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="bi bi-check-circle me-2"></i>@TempData["Success"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }
                
                <form asp-action="Create" method="post" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group text-center">
                        <img id="LogoPreview" src="/images/default-cinema.png" alt="Logo Preview" style="max-width: 200px;" class="rounded" />
                    </div>

                    <div class="form-group">
                        <label asp-for="ImageFile" class="control-label">Cinema Logo (Optional)</label>
                        <input asp-for="ImageFile" type="file" class="form-control" id="ImageFile" accept="image/*" />
                        <small class="form-text text-muted">Upload an image file (JPG, PNG, GIF, WebP) - Max 5MB (Optional)</small>
                    </div>

                    <div class="form-group">
                        <label asp-for="FullName" class="control-label"></label>
                        <input asp-for="FullName" class="form-control" />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-outline-success float-right" />
                        <a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function() {
            // Image file preview functionality
            $('#ImageFile').on('change', function() {
                var file = this.files[0];
                var preview = $('#LogoPreview');
                
                if (file) {
                    // Validate file type
                    var allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/gif', 'image/bmp', 'image/webp'];
                    if (!allowedTypes.includes(file.type)) {
                        alert('Please select a valid image file (JPG, PNG, GIF, BMP, WebP)');
                        $(this).val('');
                        preview.attr('src', '/images/default-cinema.png');
                        return;
                    }
                    
                    // Validate file size (5MB)
                    if (file.size > 5 * 1024 * 1024) {
                        alert('File size must be less than 5MB');
                        $(this).val('');
                        preview.attr('src', '/images/default-cinema.png');
                        return;
                    }
                    
                    // Show preview
                    var reader = new FileReader();
                    reader.onload = function(e) {
                        preview.attr('src', e.target.result);
                    };
                    reader.readAsDataURL(file);
                } else {
                    preview.attr('src', '/images/default-cinema.png');
                }
            });
        });
    </script>
}
